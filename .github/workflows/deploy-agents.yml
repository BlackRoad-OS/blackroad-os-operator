name: Deploy Agents to Railway

on:
  push:
    branches:
      - dev
      - staging
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - branch: dev
            environment: dev
            health_url: https://dev.agents.blackroad.systems/health
          - branch: staging
            environment: staging
            health_url: https://staging.agents.blackroad.systems/health
          - branch: main
            environment: prod
            health_url: https://agents.blackroad.systems/health
    if: github.ref_name == matrix.branch
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy agents-api and agents-worker
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          railway up --service agents-api --environment ${{ matrix.environment }} --ci
          railway up --service agents-worker --environment ${{ matrix.environment }} --ci

      - name: Health check agents-api
        run: |
          curl -f ${{ matrix.health_url }}
