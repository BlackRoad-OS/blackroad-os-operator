[build]
builder = "NIXPACKS"

[deploy]
numReplicas = 1
startCommand = "uvicorn br_operator.main:app --host 0.0.0.0 --port ${PORT:-4000}"
healthcheckPath = "/health"

[variables]
PORT = "4000"
CATALOG_PATH = "/app/agent-catalog/agents.yaml"
